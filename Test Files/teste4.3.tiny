writeStr(Entre com o numero de linhas: );
read(n);
l := 1;
while l n 1 + < do 
  i := 0;
  while i n l - < do
    writeStr(\b);
    i := i 1 +;
  endw
  for i := 1 to 2 l * 1 - do
    writeStr(*);
  endf
  writeln;
  l := l 1 +;
endw
endp
